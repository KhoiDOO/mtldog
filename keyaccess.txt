model architecture:
    - hps
        - encoder: arch_<ds>_<arch_type>_<backbone>_encoder
        - decoder: arch_<ds>_<task>_<arch_type>_<backbone>_decoder
    - cross: arch_<ds>_cross

loss function:
    - loss_<task>_<loss_name>

metric:
    - metric_<task>_<metric_name>

dataset:
    - ds_<dataset_name>

algorithm:
    - algo_<algo_name>